@page "/stories"
@model IndexModel

<partial name="_PageHero" model='@(("Long-form Archive", "Stories", "In-depth features, essays and editorial packages from the preserved Queenzone archive.", "100+ features"))' />

<section class="qz-section">
  <div class="qz-container">
    <partial name="_StoriesArchiveList" model="Model.Items" />

    @Html.Raw(StoriesRoutes.BuildArchivePaginationNav(Model.CurrentPage, Model.TotalPages))
  </div>
</section>