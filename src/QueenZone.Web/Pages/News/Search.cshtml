@page "/news/search"
@model NewsSearchModel
@*
  This page always redirects to the unified /search page (see NewsSearchModel.OnGet) —
  the route is kept only so existing inbound links to /news/search keep working.
*@
