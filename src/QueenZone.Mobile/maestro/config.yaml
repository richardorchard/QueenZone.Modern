# Shared Maestro defaults for Android emulator + iOS Simulator (#872 Option A).
# The suite entry point is smoke.yaml. Platform overlays belong in flows/
# only when Android and iOS truly differ.
appId: org.queenzone.mobile
name: QueenZone device smoke
